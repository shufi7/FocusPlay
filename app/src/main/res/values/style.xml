<?xml version="1.0" encoding="utf-8"?>
<resources>
    <style name="FocusPlayBottomSheetDialog" parent="Theme.Design.Light.BottomSheetDialog">
        <item name="bottomSheetStyle">@style/FocusPlayBottomSheet</item>
    </style>

    <style name="FocusPlayBottomSheet" parent="Widget.Design.BottomSheet.Modal">
        <item name="android:background">@android:color/transparent</item>
    </style>

    <style name="BottomRoundedHeroImage">
        <item name="cornerFamily">rounded</item>

        <!-- atas tetap siku -->
        <item name="cornerSizeTopLeft">0dp</item>
        <item name="cornerSizeTopRight">0dp</item>

        <!-- bawah dibuat rounded -->
        <item name="cornerSizeBottomLeft">32dp</item>
        <item name="cornerSizeBottomRight">32dp</item>
    </style>
</resources>

